# خلاصه وضعیت پروژه تهران اینونتوری

تاریخ به‌روزرسانی: ۳۰ اوت ۲۰۲۶

## هدف پروژه

مدیریت موجودی محصولات از طریق بات تلگرام، با امکان ثبت محصول توسط ادمین، جستجو و مشاهده محصول، محاسبه قیمت نهایی به تومان و آماده‌سازی مسیر مشتری برای جستجو و سفارش.

## وضعیت فعلی

### بات تلگرام

- منوی دکمه‌ای برای ادمین و کاربر
- ثبت محصول مرحله‌ای
- ثبت سریع محصول با یک پیام متنی
- پشتیبانی از متن چندخطی و متن پیوسته
- استخراج نام، قیمت دلار، وزن، سایز، دسته‌بندی، موقعیت و توضیحات
- تشخیص خودکار دسته‌بندی از عنوان و توضیحات
- موقعیت پیش‌فرض: تهران
- امکان ارسال یک تا سه عکس برای محصول
- پیش‌نمایش محصول قبل از ثبت
- محاسبه قیمت نهایی به تومان بر اساس:

```text
قیمت دلار × نرخ دلار × ضریب
+ وزن به کیلوگرم × هزینه حمل هر کیلو
```

- امکان ویرایش فیلدهای ثبت سریع قبل از ثبت نهایی
- لغو فرایند در مراحل مختلف ثبت
- لیست محصولات دسته‌بندی‌شده
- جستجوی متنی و تطبیق فارسی/انگلیسی
- مدیریت ادمین‌ها و دسترسی دسته‌بندی
- ثبت مالک محصول و ادمین ایجادکننده
- گزارش پایه فعالیت ادمین‌ها

### دسته‌بندی‌های فعلی

- کفش
- کیف
- لباس
- ورزشی
- آرایشی بهداشتی
- اکسسوری
- دارو و سلامتی
- عطر و ادکلن

## ساختار فنی

- `bot.py`: منطق بات، منوها، ثبت محصول، جستجو و مدیریت ادمین
- `main.py`: API با FastAPI
- `models.py`: مدل‌های SQLAlchemy
- `schemas.py`: مدل‌های ورودی و خروجی API
- `database.py`: اتصال دیتابیس
- `photos/`: فایل‌های عکس محصولات
- `admins.json`: تنظیمات ادمین‌های دارای دسترسی

## نقش‌ها و دسترسی‌ها

- سوپر ادمین:
  - مدیریت ادمین‌ها
  - تخصیص دسته‌بندی
  - مشاهده گزارش فعالیت
  - مدیریت همه محصولات
- ادمین عادی:
  - ثبت محصول
  - ویرایش محصولات مجاز
  - مدیریت محصولات بر اساس دسترسی دسته‌بندی

هر محصول فیلدهای مالک محصول و ادمین ایجادکننده را دارد.

## دیتای فعلی محیط تست محلی

از فایل خروجی تلگرام زیر استخراج انجام شده:

```text
C:\Users\Hamid Moosavi\Downloads\Telegram Desktop\ChatExport_2026-08-30 (1)
```

نتیجه استخراج:

- کاندیداها: ۱۲۹ محصول
- محصولات جدید اضافه‌شده: ۱۱۰ محصول
- موارد تکراری: ۱۹ محصول
- عکس‌های کپی‌شده: ۱۱۰ عکس
- مجموع محصولات دیتابیس تست: ۳۴۵ محصول

اسکریپت ورود:

```text
import_export_2026_08_30_local.py
```

این ورود فقط روی دیتابیس محلی انجام شده و به Railway منتقل نشده است.

## اجرای محلی

ترمینال اول، API:

```powershell
cd C:\projects\TEHRAN-INVENTORY
venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

ترمینال دوم، بات:

```powershell
cd C:\projects\TEHRAN-INVENTORY
venv\Scripts\python.exe bot.py
```

اگر پورت ۸۰۰۰ قبلاً استفاده شده باشد، API از قبل در حال اجراست و نباید دوباره اجرا شود.

## استقرار

- مخزن GitHub:
  - `shmoosavib-wq/tehraninventory`
- سرویس API روی Railway ساخته شده است.
- سرویس Bot نیز برای استقرار روی Railway آماده/منتشر شده است.
- در سرویس Bot باید مقدار زیر به دامنه عمومی API اشاره کند:

```env
API_BASE_URL=https://tehraninventory-production.up.railway.app
```

- مقدار `API_BASE_URL` نباید `localhost` یا `127.0.0.1` باشد.
- بعد از تغییر متغیرهای محیطی باید سرویس Bot redeploy یا restart شود.

## آخرین commit

آخرین commit مربوط به اصلاح ثبت سریع:

```text
fee215e Improve_quick_product_entry_parsing_and_validation
```

## نکات و ایرادهای باقی‌مانده

- parser متن پیوسته هنوز برای بعضی متن‌های بدون برچسب می‌تواند ابهام داشته باشد؛ روش پیشنهادی ورود، هر فیلد در یک خط است.
- پیام چند محصول در یک پیام باید جدا شود؛ بات برای بیش از یک قیمت هشدار می‌دهد.
- پیش‌نمایش و ویرایش ثبت سریع نیاز به تست کامل در Telegram دارد.
- نام‌گذاری بعضی محصولات استخراج‌شده از export به‌دلیل ساختار آزاد پیام‌ها ممکن است نیاز به بازبینی دستی داشته باشد.
- بسیاری از عکس‌های دیتابیس محلی هنوز باید در محیط Railway/فضای ذخیره‌سازی پایدار منتقل شوند.
- تحلیل رفتار مشتری، تقاضا، کلیک خرید و داشبورد وب در فاز بعدی قرار دارد.
- احراز هویت پیشرفته فعلاً در اولویت نیست.

## اولویت‌های پیشنهادی بعدی

1. تست نهایی ثبت سریع در بات اصلی Railway
2. بررسی چند محصول نمونه و اصلاح نام/دسته/موقعیت
3. انتقال دیتای تأییدشده به دیتابیس Railway
4. پایدارسازی ذخیره عکس‌ها
5. تکمیل فلوی مشتری: جستجو، مشاهده جزئیات و ثبت سفارش
6. ثبت رویدادهای سبک برای تحلیل تقاضا و فروش
7. ساخت داشبورد وب برای سوپر ادمین
